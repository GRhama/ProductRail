# Example — New Product

We want to create a simple B2B approval service where requests are submitted, reviewed and approved or rejected.

Known constraint: some requests may require two approvals.

Use `product-grill` to identify the minimum behavioral decisions required before writing a Feature Spec.
