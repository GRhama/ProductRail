# Example — New Feature

Existing product: document processing platform.

Feature request: allow a user to replace a document after submission.

Known behavior: submitted documents are currently immutable.

Use `product-grill` first. Do not assume versioning, overwrite semantics or audit behavior.
